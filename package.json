{
  "name": "kefir.combines",
  "version": "4.1.4",
  "description": "Special purpose applicative Kefir combinator",
  "main": "dist/kefir.combines.cjs.js",
  "module": "dist/kefir.combines.es.js",
  "scripts": {
    "dist": "npm run dist:umd & npm run dist:umd.min & npm run dist:es & npm run dist:cjs & wait",
    "dist:cjs": "    NODE_ENV=           rollup -c -n kefir.combines -i src/kefir.combines.js -f cjs -o dist/kefir.combines.cjs.js",
    "dist:es": "     NODE_ENV=           rollup -c -n kefir.combines -i src/kefir.combines.js -f es  -o dist/kefir.combines.es.js",
    "dist:umd": "    NODE_ENV=dev        rollup -c -n kefir.combines -i src/kefir.combines.js -f umd -o dist/kefir.combines.js",
    "dist:umd.min": "NODE_ENV=production rollup -c -n kefir.combines -i src/kefir.combines.js -f umd -o dist/kefir.combines.min.js",
    "lint": "eslint src test",
    "prepublish": "npm run lint && npm run dist && npm run test",
    "report-coverage": "nyc report --reporter=lcov > coverage.lcov && codecov",
    "test": "nyc mocha"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/calmm-js/kefir.combines.git"
  },
  "keywords": [
    "kefir"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/calmm-js/kefir.combines/issues"
  },
  "homepage": "https://github.com/calmm-js/kefir.combines#readme",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.1",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-3": "^6.24.1",
    "babel-register": "^6.24.1",
    "codecov": "^2.1.0",
    "eslint": "^3.19.0",
    "kefir": "^3.7.1",
    "mocha": "^3.2.0",
    "nyc": "^10.2.0",
    "ramda": "^0.23.0",
    "rollup": "^0.41.6",
    "rollup-plugin-babel": "^2.7.1",
    "rollup-plugin-node-resolve": "^3.0.0",
    "rollup-plugin-replace": "^1.1.1",
    "rollup-plugin-uglify": "^1.0.1"
  },
  "dependencies": {
    "infestines": "^0.4.4"
  },
  "peerDependencies": {
    "kefir": "^3.7.0"
  }
}
